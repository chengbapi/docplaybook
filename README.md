# docplaybook workspace

This repository uses a `pnpm workspace` layout.

## Packages

- `packages/docplaybook`: the publishable CLI package
- `apps/docs`: the Rspress documentation site

## Common commands

```bash
pnpm install
pnpm build
pnpm test
pnpm docs:dev
```
